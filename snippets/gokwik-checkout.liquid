{% if settings.goEnable and settings.mid != blank and settings.envType != blank %}
<div class="gokwik-checkout"{% if settings.hideFlow %} style="display:none !important;" {% endif %}>
<button type='button' class="button disabled" disabled onclick="onCheckoutClick(this)">
  <span class="btn-text">
    <span>{{ settings.main-titile | default: 'Checkout' }}</span>
    <span>{{ settings.sub-titile | default: '' }}</span>
  </span>
  {% if settings.enableLogo %}
  <span class="pay-opt-icon">
   <img src="https://cdn.gokwik.co/v4/images/upi-icons.svg" width="84" height="24" alt="">
    <img src="https://cdn.gokwik.co/v4/images/right-arrow.svg" width="16" height="16" alt="">
  </span>
  {% endif %}
  <div style="display:none" class="addloadr" id="btn-loader">
    <div class="cir-loader">Loading..</div>
  </div>
</button>
</div>
{% endif %}
